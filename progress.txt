## Session - 2026-05-20/21 — Document Portal: built, deployed, ready to demo

**Where it lives:**
- App: `app-suitemanager/` (nested in this repo)
- Git: pushed to `realjbmangum/site-suitemanagerllc` `main`; CF Pages auto-deploys on push
- Project: `app-suitemanagerllc` on Cloudflare Pages → `app-suitemanagerllc.pages.dev` (custom domain `app.suitemanagerllc.com` pending DNS)
- D1: `suitemanager-portal` (id `a05dcf90-...4737d8`)
- R2: `suitemanager-portal-files`

**Auth + onboarding (shipped):**
- PBKDF2-SHA256 100k iters (`src/lib/crypto.ts`)
- D1-backed sessions, HttpOnly+Lax cookie, 12h TTL (`src/lib/session.ts`)
- `src/middleware.ts`: route gating per role
- Magic-link invite flow: admin creates user (NULL password + invite_token), user clicks `/invite/:token`, sets password via `/api/auth/activate`, gets session immediately. Invite tokens 7-day TTL.
- `/api/auth/login`, `/api/auth/logout`, `/api/auth/activate`
- Seed script `scripts/create-user.mjs` (UPSERT-by-email) for admins; magic link for everyone else

**Roles (final):** `gm` | `strand` | `admin`
- gm: own-property upload + Resources (read-only)
- strand: Dashboard + Resources + HR Templates (manages both invoices and HR templates — Karen lives here too)
- admin: everything (incl. /admin/properties, /admin/users, /admin/email-preview)
- The `hr` role we briefly built was collapsed into `strand` (May 21) — separation-of-duties wasn't worth the role-management overhead at this scale

**Admin surfaces:**
- `/admin/users`: list + create (magic-link invite with copy-link UX) + edit (`/admin/users/[id]`) + delete (clears sessions + audit_events first to satisfy FK)
- `/admin/properties`: 5 tiles (count, total rooms, states, avg rooms, Need-a-GM); brand/state/owner filters; sortable columns; row → detail
- `/admin/properties/[id]`: full property profile with rooms, total docs, inbox count, invoice 30d $, recent docs list, assigned GMs, contact card
- `/admin/templates`: HR/strand folder + template management (versioned, soft-archive via archived_at)
- `/admin/email-preview`: live iframe preview of the invite email template, role variants (Graph mail wiring deferred)

**Document flow (shipped end-to-end against R2 + D1):**
- `/upload` (GM): drag-drop, category (invoice/statement/other), vendor, amount, note → POST `/api/upload` writes blob to R2 + row to documents + audit_events
- `/dashboard` (strand/admin): status tabs (Inbox/Reviewed/All — default Inbox), tiles (Today/Week/Pending review/Storage), category pills with counts, property selector, free-text search, sortable columns, per-row Mark reviewed / Reopen / Download
- `/dashboard/[id]`: inline PDF/image preview via `?inline=1`, GM note in brass call-out, vendor/amount/timestamps, action buttons, full audit timeline
- `/api/documents/[id]/download` + `/review` (audit-logged)
- `/resources` (everyone): GM browse + download
- `/api/resources` + `/api/resources/folders` + `/api/resources/[id]/download`

**Data:**
- Properties schema expanded (May 20): code, brand, owner_group, parsed address (street/city/state/zip), room_count, phone, fax, emergency_phone, property_email
- 33 ASA properties seeded from `data/ASA Location Phone List 2026.xlsx` (gitignored). Total 1,289 rooms.
- Seed script at `scripts/seed-properties.mjs` (xlsx parser + auto-fix: Salibury→Salisbury, Harrisonburg CHORUM nulled to prevent collision with Greenville)
- Test placeholder property + test docs + R2 blobs cleaned up

**Brand + visual:**
- Tailwind v4, navy `#1D1D33` + brass `#BB945C` + cream `#F5F4F1`
- Real Suite Manager logo (logo1.png variant from marketing site) on login + invite + every page header (white-on-navy)
- DM Serif Display for page titles, Inter for body/UI

**Security posture (current):**
- Internet-facing `/login`. PBKDF2 makes brute force slow; no rate limit yet.
- Cloudflare Access was considered (set up briefly, then torn down — allowlist maintenance would be brittle as Chris adds properties)
- Recommended hardening still TODO: rate limit on `/api/auth/login` + `/api/auth/activate`, optional Turnstile on login form, app-level account lockout

**Email:**
- Microsoft Graph chosen as transport (Suite Manager M365 tenant). Wiring deferred.
- `src/lib/email/templates.ts` has `buildInviteEmail()` ready — table-based + inline styles, system fonts; loads logo from public marketing site URL (so Access wouldn't block it if reintroduced)
- `/admin/email-preview` renders live preview for iteration

**Notable bugs caught + fixed:**
- `password_hash NOT NULL` blocked invite-pending users → migrated table to nullable (May 20)
- D1 partial unique index didn't satisfy `ON CONFLICT(code)` → switched to full unique index (multi-NULL still allowed by SQLite default)
- `/api/users/:id/delete` failed on users with audit history (FK to audit_events.user_id) → endpoint now clears audit rows before deleting
- Inline doc preview was spamming audit log with download events → preview path (`?inline=1`) now skips audit write

**Files of note (new this session):**
- `src/lib/{ids,crypto,session,audit,files}.ts`, `src/lib/email/templates.ts`
- `src/middleware.ts`
- `src/layouts/PortalLayout.astro`
- `src/pages/{index,login,upload,resources,dashboard}.astro`
- `src/pages/dashboard/[id].astro`
- `src/pages/invite/[token].astro`
- `src/pages/admin/{users,properties,templates,email-preview}.astro`
- `src/pages/admin/{users,properties}/[id].astro`
- `src/pages/api/auth/{login,logout,activate}.ts`
- `src/pages/api/users.ts` + `src/pages/api/users/[id]/{update,delete}.ts`
- `src/pages/api/properties.ts`
- `src/pages/api/upload.ts`
- `src/pages/api/documents/[id]/{download,review}.ts`
- `src/pages/api/resources/{index,folders}.ts` + `src/pages/api/resources/[id]/download.ts`
- `scripts/{create-user,seed-properties}.mjs`
- `worker/db/schema.sql`

**Pending (next sessions):**
- Wire Microsoft Graph for invite + upload-notification emails
- Rate limiting on auth endpoints (CF WAF rule + app-level lockout)
- Bulk-invite GMs from property list
- Property selector on GM upload-form recent-uploads strip
- Vendors table (deferred — Brian said not yet)
- Optional: cleanup remaining "hr" CHECK-constraint relic in DB

**Decisions logged this session:**
- Per-GM logins, not per-property — needed for clean audit attribution
- Magic-link only path for v1 (no admin-set passwords); admins seed via `create-user.mjs`
- Strand = single role covering accounting + HR (folded `hr` in May 21)
- Properties is admin-only (nav + middleware gates both confirmed)
- Email is MS Graph, deferred — not SendGrid

---

## Session - 2026-05-20 — Document Portal scaffold

**Completed:**
- Wrote PRD / TECHNICAL-SPEC / DESIGN-BRIEF for the Suite Manager Document Portal (`tasks/`)
- Wrote paste-ready proposal copy (`tasks/PROPOSAL-copy.md`) — $9,500 fixed + $35/property/mo
- Shipped rich-text rendering + redesigned SOW layout to the Ascend Systems proposal generator (commit `085ac16` in `ascend-systems`): bullets, numbered lists, bold, numbered section markers, Investment card
- v2 doc updates per Karen (Strand HR) email: dropped GM-uploaded HR docs from v1; added HR Resources library (HR → GM template distribution). Categories now Invoice/Statement/Other.
- Scaffolded standalone Astro + Cloudflare Pages app at `app-suitemanager/`:
  - Tailwind v4 with brand tokens (navy #1D1D33, brass #BB945C)
  - PortalLayout with branded header
  - Routes: /login, /upload, /resources, /dashboard, /admin/templates (static UI, placeholder data matches mockups)
  - wrangler.toml with D1 + R2 binding placeholders
  - worker/db/schema.sql with users/properties/documents/resources/resource_folders/sessions/audit_events
  - `npm run build` passes clean

**Files (new):**
- `tasks/PRD-document-portal.md` (v2), `tasks/TECHNICAL-SPEC.md` (v2), `tasks/DESIGN-BRIEF.md` (v2)
- `tasks/PROPOSAL-copy.md`
- `app-suitemanager/` — full Astro scaffold
- `app-suitemanager/mockups/01-gm-upload.png`, `02-strand-dashboard.png`

**Decisions:**
- Portal lives at `app.suitemanagerllc.com`, standalone — nested inside `site-suitemanagerllc/app-suitemanager/`, deployed to its own Cloudflare Pages project
- HR scope reversed: templates library (HR → GM read-only) instead of GM HR uploads. Engage handles inbound HR docs.
- Per-GM logins (not per-property) — needed for HR Resources audit trail and per-GM upload attribution
- Roles: `gm` | `strand` | `hr` | `admin`. HR has zero visibility into GM document uploads.

**Auth shipped (same session, May 20 PM):**
- D1 db `suitemanager-portal` (id `a05dcf90-...4737d8`) linked in wrangler.toml
- R2 bucket `suitemanager-portal-files` created (remote)
- Schema applied to both remote and local D1
- Auth: PBKDF2-SHA256 100k iters (`src/lib/crypto.ts`), session tokens in D1 + HttpOnly Lax cookie (`src/lib/session.ts`), `src/middleware.ts` enforces auth + role gating, `/api/auth/login` + `/api/auth/logout` endpoints
- Login page handles error query param + `next` redirect
- PortalLayout reads `Astro.locals.user` and shows Sign out
- Seed script `scripts/create-user.mjs` — PBKDF2 hash + UPSERT-on-email SQL, repeatable
- Brian seeded as admin (email `bmangum1@gmail.com`, temp password `Suite!Admin#2026` — change immediately)
- `npm run build` passes; sharp warning fixed via `imageService: 'compile'`

**Pending (next sessions):**
- [ ] /api/upload + R2 storage routing
- [ ] Email notification routing (Strand Accounting / SM admin)
- [ ] /api/documents + dashboard wiring + filters + search
- [ ] HR Resources API + folder/template CRUD
- [ ] Audit log writes on every upload/download
- [ ] Role-based query scoping (especially HR silo from documents)
- [ ] Custom domain `app.suitemanagerllc.com` + secrets
- [ ] Reply to Karen with the Resources screenshot once it's wired

**Gotchas:**
- Astro Cloudflare adapter auto-enables session KV binding — warning is benign for now (we're rolling our own session via D1), but address before first deploy by either adding a KV binding or disabling Astro's sessions feature.
- Sharp warning at build: set `imageService: 'compile'` in the adapter config to silence.

---

## Session - 2026-02-26

**Completed:**
- Built full 4-page Astro + Tailwind site for Suite Manager LLC
- Competitor research: 7 hotel management company sites analyzed
- Design direction: DM Serif Display + Inter, navy (#1A1A2E) + gold (#B8955A) + cream (#F8F6F1)
- Pages: Home, Services, About, Contact
- Components: Nav (transparent→solid scroll), Footer, HeroCTA
- Hero: video background (hero.mp4, extended stay property, daytime), centered layout
- Logo.png placed above hero headline (h-40 mobile / h-56 desktop, inverted white)
- Trust bar: 4 icon cards (building, shield, home, chart icons)
- Copy: benefits-focused, no litotes, company voice (not personal)
- Created GitHub repo: github.com/realjbmangum/site-suitemanagerllc
- Deployed to Cloudflare Pages, connected suitemanagerllc.com

**Key Fix:**
- global.css was never imported in BaseLayout.astro — nothing was styled until this was caught
- btn-primary/btn-ghost rewritten as vanilla CSS (no @apply) to guarantee rendering

**Files:**
- src/layouts/BaseLayout.astro — imports global.css (critical fix)
- src/styles/global.css — all button styles, animations, utilities
- src/pages/index.astro — hero with logo + video bg, trust bar, services, how-we-work, CTA
- src/pages/services.astro — 6 service sections alternating layout
- src/pages/about.astro — principles, story, values
- src/pages/contact.astro — 3 contact options + Web3Forms form
- src/components/Nav.astro — text wordmark, transparent→solid, gold progress bar
- src/components/Footer.astro — logo image (inverted white), nav links, contact
- src/components/HeroCTA.astro — reusable dark CTA section
- public/logo.png — logo image
- public/hero.mp4 — hero background video

**Placeholders Chris Still Needs to Fill:**
- [ ] Real phone number (currently (555) 000-0000)
- [ ] Real email (currently hello@suitemanagerllc.com)
- [ ] Web3Forms access key (contact form won't submit without it)
- [ ] Calendly link for "Schedule a Call" button on contact page
- [ ] Real bio/background for About page Our Story section
- [ ] City, State for footer location blurb
- [ ] Headshot photo (placeholder shows "CM" initials)

**Decisions:**
- Nav uses CSS filter (brightness invert) to flip logo white on dark hero — no separate white logo needed
- btn-primary = gold bg (#B8955A) + white text — rendered as vanilla CSS not @apply
- Hero content padding zeroed out — section min-h-[90vh] handles vertical centering
- No body padding-top — hero sections use internal padding to clear fixed nav

**Next Steps:**
- [ ] Chris to fill in all placeholder content above
- [ ] Set up Web3Forms account and add access key
- [ ] Add Calendly link
- [ ] Consider adding a testimonials section once Chris has client quotes
- [ ] Google Analytics / Cloudflare Analytics setup
