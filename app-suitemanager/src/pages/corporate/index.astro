---
// /corporate → land on the Calendar tab by default (the most-used corporate
// surface). Documents and future builds are siblings under /corporate/.
export const prerender = false;
return Astro.redirect("/corporate/calendar");
---
