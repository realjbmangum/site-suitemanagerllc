---
// Approvals moved into the dashboard as a tab. Keep this path alive so any
// stale links / bookmarks land in the right place.
export const prerender = false;
return Astro.redirect("/dashboard?tab=approvals");
---
